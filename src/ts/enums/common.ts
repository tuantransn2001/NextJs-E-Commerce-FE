export enum AUTH_TYPE {
  LOGIN = 'login',
  REGISTER = 'register',
}

export enum BUTTON_TYPE {
  primary = 'primary',
}
export enum BUTTON_ACTION_TYPE {
  button = 'button',
  submit = 'submit',
  reset = 'reset',
}

export enum HREF {
  home = '/home',
  auth = '/auth',
  info = '/info',
}
